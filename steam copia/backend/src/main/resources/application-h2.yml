spring:
  datasource:
    url: jdbc:h2:mem:steam_copia;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DATABASE_TO_UPPER=false
    driverClassName: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
  flyway:
    locations: classpath:db/migration-h2

